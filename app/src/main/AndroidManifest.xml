
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gramavaxi">

    <application
        android:allowBackup="true"
        android:label="Grama-Vaxi"
        android:theme="@style/Theme.GramaVaxi">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".RegisterAnimalActivity" />
        <activity android:name=".HealthRecordsActivity" />
        <activity android:name=".HealthGuideActivity" />
        <activity android:name=".ProfileActivity" />

    </application>

</manifest>
